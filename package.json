{
  "name": "xeragent-workspace",
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install && cd frontend && npm install && cd ../electron && npm install",
    "dev-backend": "node scripts/start-backend.js",
    "dev-frontend": "cd frontend && npm run dev",
    "dev-electron": "cd electron && npm start",
    "dev": "concurrently \"npm run dev-backend\" \"npm run dev-frontend\" \"npx wait-on http://localhost:5173 && npm run dev-electron\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "wait-on": "^7.2.0"
  }
}
